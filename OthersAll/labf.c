#include<stdio.h>
int main()
{
int maths[10];
scanf("%d",&maths[10]);
if(maths<=10){
    printf("Below than or Equal to10: %d\n",maths);}
else if(maths>10){
    prinif("Greater than 10: %d\n",maths);}
return 0;
}
